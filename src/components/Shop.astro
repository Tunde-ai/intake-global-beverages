<!-- ═══ Shop — Product Carousel ═══ -->
<section class="shop" id="shop">
  <div class="shop__bg"></div>
  <div class="shop__overlay"></div>
  <div class="shop__container container">
    <h1 class="sr-only">Shop</h1>
    <div class="shop__carousel" id="shop-carousel">
      <div class="shop__track" id="shop-track">
        <figure class="shop__slide">
          <img src="/intake-global-beverages/assets/solo-cans/rescue-side-1.png" alt="InTake Rescue Raspberry Aid" class="shop__image">
          <img src="/intake-global-beverages/assets/solo-cans/rescue-side-2.png" alt="InTake Rescue Raspberry Aid — back" class="shop__image">
        </figure>
        <figure class="shop__slide">
          <img src="/intake-global-beverages/assets/solo-cans/paris-side-1.png" alt="InTake Paris Apricot Mist" class="shop__image">
          <img src="/intake-global-beverages/assets/solo-cans/paris-side-2.png" alt="InTake Paris Apricot Mist — back" class="shop__image">
        </figure>
        <figure class="shop__slide">
          <img src="/intake-global-beverages/assets/solo-cans/sport-inverse-side-1.png" alt="InTake Sport Blueberry Ice" class="shop__image">
          <img src="/intake-global-beverages/assets/solo-cans/sport-inverse-side-2.png" alt="InTake Sport Blueberry Ice — back" class="shop__image">
        </figure>
      </div>
      <button class="shop__arrow shop__arrow--prev" id="shop-prev" aria-label="Previous product">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor"><path d="M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z"/></svg>
      </button>
      <button class="shop__arrow shop__arrow--next" id="shop-next" aria-label="Next product">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor"><path d="M10 6L8.59 7.41 13.17 12l-4.58 4.59L10 18l6-6z"/></svg>
      </button>
    </div>
  </div>
</section>

<!-- Caption bar sits OUTSIDE the masked shop section -->
<div class="shop-caption-wrapper" id="shop-caption-wrapper">
  <div class="shop-caption-inner">
    <div class="shop-caption__bar">
      <div class="shop-caption__track" id="shop-caption-track">
        <div class="shop-caption__slide">InTake Rescue Raspberry Aid</div>
        <div class="shop-caption__slide">InTake Paris Apricot Mist</div>
        <div class="shop-caption__slide">InTake Sport Blueberry Ice</div>
      </div>
    </div>
  </div>
</div>

<style>
.shop {
  position: relative;
  z-index: 3;
  width: 100%;
  aspect-ratio: 16 / 9;
  min-height: 700px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: visible;
  margin-top: -300px;
  padding-top: 350px;
  -webkit-mask-image: linear-gradient(to bottom, transparent 0%, black 15%, black 100%);
  mask-image: linear-gradient(to bottom, transparent 0%, black 15%, black 100%);
}
.shop__bg {
  position: absolute;
  inset: 0;
  background-image: url('/intake-global-beverages/assets/backgrounds/waterfall-poster.png');
  background-size: cover;
  background-position: center top;
  background-repeat: no-repeat;
}
.shop__overlay {
  position: absolute;
  inset: 0;
  background: rgba(15, 39, 87, 0.5);
}
.shop__container {
  position: relative;
  z-index: 2;
  max-width: 1140px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 2rem;
  margin-top: -5%;
}
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}
.shop__carousel {
  position: relative;
  width: 660px;
  max-width: 100%;
  overflow: hidden;
  cursor: grab;
}
.shop__carousel:active { cursor: grabbing; }

/* Feathered edges — transparent fade, not darkening */
.shop__feather {
  position: absolute;
  top: 0;
  bottom: 0;
  width: 80px;
  z-index: 3;
  pointer-events: none;
}
.shop__feather--left {
  left: 0;
  -webkit-mask-image: none;
}
.shop__feather--right {
  right: 0;
  -webkit-mask-image: none;
}
/* Apply fade mask to the track instead */
.shop__carousel {
  -webkit-mask-image: linear-gradient(to right, transparent 0%, black 10%, black 90%, transparent 100%);
  mask-image: linear-gradient(to right, transparent 0%, black 10%, black 90%, transparent 100%);
}

.shop__track {
  display: flex;
  transition: transform 0.5s ease;
  user-select: none;
}
.shop__slide {
  min-width: 100%;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 1.5rem;
  text-align: center;
}
.shop__image {
  height: 600px;
  width: auto;
  max-width: 44%;
  object-fit: contain;
  display: block;
  pointer-events: none;
}
/* Caption wrapper — independent layer above all sections */
.shop-caption-wrapper {
  position: relative;
  z-index: 20;
  margin-top: -80px;
  pointer-events: none;
}
.shop-caption-inner {
  max-width: 660px;
  margin: 0 auto;
  pointer-events: auto;
}
.shop-caption__bar {
  width: 100%;
  background: rgba(255, 255, 255, 0.12);
  backdrop-filter: blur(30px) saturate(1.2);
  -webkit-backdrop-filter: blur(30px) saturate(1.2);
  border-top: 1px solid rgba(255, 255, 255, 0.15);
  border-bottom: 1px solid rgba(255, 255, 255, 0.15);
  overflow: hidden;
  padding: 1rem 0;
  -webkit-mask-image: linear-gradient(to right, transparent 0%, black 10%, black 90%, transparent 100%);
  mask-image: linear-gradient(to right, transparent 0%, black 10%, black 90%, transparent 100%);
}
.shop-caption__track {
  display: flex;
  transition: transform 0.5s ease;
}
.shop-caption__slide {
  min-width: 100%;
  font-family: 'Bebas Neue', cursive;
  font-size: 45px;
  font-weight: 400;
  line-height: 1.1;
  letter-spacing: 4px;
  color: var(--white);
  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
  text-align: center;
}
.shop__arrow {
  position: absolute;
  top: 45%;
  transform: translateY(-50%);
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: rgba(255, 255, 255, 0.9);
  cursor: pointer;
  transition: background 0.3s;
  z-index: 4;
}
.shop__arrow:hover {
  background: rgba(255, 255, 255, 0.3);
}
.shop__arrow--prev { left: -60px; }
.shop__arrow--next { right: -60px; }

@media (max-width: 768px) {
  .shop {
    min-height: auto;
    aspect-ratio: auto;
    padding: 100px 20px 60px;
  }
  .shop__container { min-height: auto; margin-top: 0; }
  .shop__carousel { width: 100%; }
  .shop__image { height: 280px; max-width: 46%; }
  .shop-caption__slide { font-size: 28px; letter-spacing: 2px; }
  .shop-caption-inner { max-width: 90%; }
  .shop__arrow--prev { left: 5px; }
  .shop__arrow--next { right: 5px; }
}
</style>
