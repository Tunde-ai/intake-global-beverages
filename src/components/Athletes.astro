<!-- ═══ 4. Athlete Banner ═══ -->
<section class="athletes fade-in" id="athletes">
  <div class="athletes__grid">
    <div class="athletes__panel">
      <img src="/intake-global-beverages/assets/athletes/increase-01.jpg" alt="Female runner — Increases" class="athletes__photo">
      <div class="athletes__text">
        <h2 class="athletes__heading">INCREASES:</h2>
        <p class="athletes__sub">Energy, Hydration, Vitality</p>
      </div>
    </div>
    <div class="athletes__panel athletes__panel--sustains">
      <img src="/intake-global-beverages/assets/athletes/sustain-02.jpg" alt="Male athlete — Sustains" class="athletes__photo">
      <div class="athletes__text">
        <h2 class="athletes__heading">SUSTAINS:</h2>
        <p class="athletes__sub">Endurance, Strength, Growth</p>
      </div>
    </div>
    <div class="athletes__panel athletes__panel--reflects">
      <img src="/intake-global-beverages/assets/athletes/reflect-03.jpg" alt="Woman in dress — Reflects" class="athletes__photo">
      <div class="athletes__text">
        <h2 class="athletes__heading">REFLECTS:</h2>
        <p class="athletes__sub">Performance, Image, Elevation</p>
      </div>
    </div>
  </div>
</section>

<style>
.athletes {
  background: #000;
  width: 100%;
}
.athletes__grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  width: 100%;
}
.athletes__panel {
  position: relative;
  overflow: hidden;
}
.athletes__photo {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center top;
}
/* Sustains — scale man moderately larger */
.athletes__panel--sustains .athletes__photo {
  object-position: center 15%;
  transform: scale(1.18);
  transform-origin: center 25%;
}
.athletes__text {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  background: rgba(0, 0, 0, 0.25);
}
.athletes__heading {
  font-family: 'Goldman', sans-serif;
  font-size: 72px;
  font-weight: 400;
  font-style: italic;
  color: #489FB4;
  text-shadow: 0 3px 20px rgba(0,0,0,0.8);
  letter-spacing: 4px;
  text-transform: uppercase;
  -webkit-text-stroke: 3px #489FB4;
  paint-order: stroke fill;
}
.athletes__sub {
  font-family: 'Goldman', sans-serif;
  font-size: 20px;
  font-weight: 400;
  color: var(--white);
  text-shadow: 0 2px 12px rgba(0,0,0,0.7);
  letter-spacing: 1px;
  margin-top: 0.5rem;
}

@media (max-width: 768px) {
  .athletes__grid { grid-template-columns: 1fr; }
  .athletes__panel { max-height: none; }
  .athletes__photo { height: auto; object-fit: contain; object-position: center; }
  .athletes__panel--sustains .athletes__photo { transform: none; }
  .athletes__heading { font-size: 48px; }
  .athletes__sub { font-size: 16px; }
}

/* Landscape mobile */
@media (max-height: 500px) and (orientation: landscape) {
  .athletes__grid { grid-template-columns: 1fr; }
  .athletes__panel { max-height: 100svh; overflow: hidden; }
  .athletes__photo { width: 100%; height: 100svh; object-fit: cover; object-position: center center; }
  .athletes__panel--sustains .athletes__photo { transform: none; }
  .athletes__heading { font-size: 56px; }
  .athletes__sub { font-size: 18px; }
}
</style>
